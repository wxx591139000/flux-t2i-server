# flux-t2i-server 接交接（交接文档）

> 交接日期：2026-08-19 · 生成方式：归档整理 skill「交接准备」步骤自动产出
> 续接方式：说**「按交接提示继续」** → 读本文件 + `flux-t2i-server-resume-handoff` 记忆，对齐锚点后无缝继续

---

## 一、项目现状（✅ 已归档，无未提交改动）

| 维度 | 值 |
|---|---|
| 项目 | flux-t2i-server — FLUX.1-dev 文生图服务 |
| 版本 | **v2.0**（多服务器 + VPS 看门狗；未提交改动，见「六、本次增项」） |
| 本地 | `E:\myClaudCodeWorkspace\flux-t2i-server` |
| Git | `https://github.com/wxx591139000/flux-t2i-server.git` · 分支 `main` |
| 公网 | `https://flux.zhuanlu.xyz`（本地 :9620，经 cloudflared 隧道 `xhs-tunnel`） |
| 组成 | v1.7 飞书「图图」bot（`FeishuBot`）· v1.8 admin 登录修复 · **v2.0 多服务器 + VPS 看门狗** |

## 二、启动方式（脚本在 flux 仓库内）

| 脚本 | 作用 |
|---|---|
| `启动-01-FLUX文生图服务.bat` | 只拉 flux 服务 (+保证隧道) |
| `启动-02-小红书发布服务.bat` | 只拉 xhs 服务 (+保证隧道) |
| `启动-03-仅隧道.bat` | 只起 cloudflared 隧道 |
| `start_service.bat` / `.ps1` | 主脚本，可 `-Target flux\|xhs\|tunnel\|all` |

## 三、遗留待办（非阻塞）

- 重启脚本结尾 exit code 1：服务实测正常（已起来 + :9620 + 公网均 200），判断为脚本收尾校验步骤返回非零，属外观瑕疵，可留可修。
- **flux2 接入待克隆完成**：`autodl-flux2`（connect.weste.seetacloud.com:23192）SSH 别名已加，**免密 + 数据盘克隆校验 + VPS 看门狗部署**待 clone 就绪后补齐（见 `watchdog/README.md`）。

## 六、本次增项（v2.0 · 2026-09-04）

- `manager/flux_server_manager.py`：`FLUX_SERVERS` 多服务器注册表（默认 flux1+flux2，env `FLUX_SERVERS_JSON` 覆写）+ SSH 操作全部服务器感知 + `probe/find_ready_server/any_ready`。
- `manager/flux_queue.py`：`_generate` 每单挑任一台可用服务器 + `server` 回写；`_health_loop` 改 `any_ready()`。
- `manager/flux_db.py`：jobs 表新增可空 `server` 列（幂等迁移）。
- 新建 `watchdog/`：`flux_server_ready.sh`（服务器端就绪）、`flux_watchdog.sh`（VPS 巡检）、`flux-watchdog.service`（systemd）、`README.md`（部署 vps-aliyun）。

## 四、硬约束（务必遵守）

1. 🔑 LLM/HF/激活码 key 读 `manager/.env`，不硬编码进仓库
2. 📄 文档改动必升版本号
3. ⏰ 判断时间用 `date`，不凭日志猜
4. 🖼️ 不直接预览图片（会卡死）；改海报/图从 HTML/文本源处理
5. 🌐 现代模型链纯文本，收到图/视频/PDF 先转文本再喂模型
6. 🚫 不改转录项目 `server-pdf-converter`
7. 🔧 改飞书/API 配置前必须先确认；GitHub 上传/下载走 `github-upload` skill；push 只在用户要求时
8. 🌉 **隧道双约定**：本地只跑 `xhs-tunnel`(27da88b4)，管 flux.zhuanlu.xyz + xhs.zhuanlu.xyz；VPS 跑 `transcribe-bot`(1779cc80)。严禁用旧 transcribe-bot 配置重启本地 cloudflared（抢隧道致 upload.zhuanlu.xyz 502）。正确启动：`cd C:\Users\Dancing\.cloudflared && cloudflared tunnel run xhs-tunnel`