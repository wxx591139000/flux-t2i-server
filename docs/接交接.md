# flux-t2i-server 接交接（交接文档）

> 交接日期：**2026-09-23** · 生成方式：归档整理 skill「交接准备」步骤自动产出
> 续接方式：说**「按交接提示继续」** → 读本文件 + `flux-t2i-server-resume-handoff` 记忆，对齐锚点后无缝继续

---

## 一、项目现状（✅ 已归档）

| 维度 | 值 |
|---|---|
| 项目 | flux-t2i-server（A 链）— FLUX 文生图服务 + 任务中心 + 商户后台 + 飞书机器人 |
| 版本 | **v2.9.0** · 2026-09-23 |
| 本地 | `E:\myClaudCodeWorkspace\flux-t2i-server`（⚠️ 伞项目 `image-platform\flux-t2i-server` 是 junction，**git 操作走真实路径**） |
| Git | `https://github.com/wxx591139000/flux-t2i-server.git` · 分支 `main` · 归档标签 **`archive-20260923`** |
| 归档时 HEAD | `94ee0a96b6`（自 `archive-20260904` 起 39 个提交） |
| 公网入口 | `flux.zhuanlu.xyz` → **客户站点（B链 3000）**<br>`flux-admin.zhuanlu.xyz` → **商户后台（本服务 9620，Token 保护）**<br>`xhs.zhuanlu.xyz` → 小红书工具 8800 |
| 本机入口 | `http://127.0.0.1:9620`（生成页）· `http://127.0.0.1:9620/admin`（商户后台，不经过隧道） |
| 机群 | 唯一事实来源 = `manager/servers.json`（换机只改 JSON，不改代码） |

## 二、启动方式

| 场景 | 入口 |
|---|---|
| **对外生产（推荐，一次点完）** | `image-platform\启动-对外生产站点.bat` → 9620 重启 + 隧道重启 + 3000 生产 + **身份验收** |
| 只应用隧道配置改动 | `image-platform\重启隧道.bat` |
| 只起本服务 / 小红书 / 仅隧道 | `启动-01-FLUX文生图服务.bat` / `启动-02-…` / `启动-03-仅隧道.bat` |
| 主脚本（可传参） | `start_service.ps1 -Target flux\|xhs\|tunnel\|all [-Restart]` |

⚠️ **一键启动的验收输出看这三处**：`image-platform\logs\last-run-progress.txt`（跑到哪一步）、
`last-run-verdict.txt`（结论）、`start_public-<时间戳>.log`（全程）。

## 三、遗留待办

**P0（会烧钱/会丢用户结果，优先）—— 图图机器人**
1. `MAX_ACTIVE=50` 超出**静默不跟踪** → 用户永远收不到图且不知情。改为必须回执
2. 无事件去重 → 飞书事件重放会**重复出图 + 重复扣额度**。加 `evt:` + `filekey:` 双键去重
3. 在途任务只在内存（`feishu_bot.py:41`）→ 重启 9620 后结果永不回传。落 DB + 启动恢复

**其他**
- `9620 /health` 不透传 `profile` / `capabilities`（P1）
- **Qwen 显存实测仍未做**（bf16 约 30-35GB vs 32G 卡）—— 最大未知数，等带卡
- **`pause` 卡死编排器**：根因（数组 splatting 绑不上 `-NoPause`）已修，
  但"真实控制台不再卡"**未能亲自验证**（工具 stdin 是管道，复现不出）。下次真实运行取证
- 图图画像 P0-P6 分阶段计划见 `docs/图图-飞书完整互动-设计方案.md`；三个决策已拍板（§8）

## 四、硬约束（务必遵守）

**既有（用户历次指定，继续有效）**
1. 🔑 LLM/HF/激活码 key 读 `manager/.env`，不硬编码进仓库
2. 📄 文档改动必升版本号
3. ⏰ 判断时间用 `date`，不凭日志猜
4. 🖼️ 不直接预览图片（会卡死）；改海报/图从 HTML/文本源处理
5. 🌐 现代模型链纯文本，收到图/视频/PDF 先转文本再喂模型
6. 🚫 不改转录项目 `server-pdf-converter`
7. 🔧 改飞书/API 配置前必须先确认；GitHub 上传/下载走 `github-upload` skill；push 只在用户要求时
8. 🌉 **隧道双约定**：本地只跑 `xhs-tunnel`(27da88b4)，管 flux/xhs/flux-admin 三个域名；
   VPS 跑 `transcribe-bot`(1779cc80)。严禁用旧 transcribe-bot 配置重启本地 cloudflared
   （抢隧道致 upload.zhuanlu.xyz 502）。正确启动：`cd C:\Users\Dancing\.cloudflared && cloudflared tunnel run xhs-tunnel`

**2026-09-23 新增**
9. ⚠️ 改 `~/.cloudflared/config.yml` 后**必须重启 cloudflared** 才生效（否则跑的是内存里的旧 ingress）；
   ingress **顺序敏感**，`http_status:404` 兜底必须最后
10. ⚠️ **禁止 `& script.ps1 @数组`** 调子脚本（`[switch]` 参数会全部绑不上 → `-NoPause` 失效 → `pause` 卡死编排器）
11. ⚠️ 任何会 `Start-Process` 的脚本，**开头先跑 `Repair-DuplicateEnv`**（本机有大小写重复的代理环境键，
    带重定向启动必抛）；对外长驻服务一律**带重定向**启动（无窗口 + 日志落盘）
12. ⚠️ 验收判据要**身份/新鲜度**，不要**活体** —— 自问「我什么都不做时它会不会也通过？」
13. ⚠️ 改 `server/` 下常驻文件 → **第一动作是 `watchdog\deploy_vps.py`**（否则被看门狗反向覆盖）

## 五、续接路径

```
说「按交接提示继续」
  → 读 .workbuddy/memory/flux-t2i-server-resume-handoff.md（锚点/待办/约束）
  → 读本文件
  → 对齐锚点：git rev-parse HEAD 是否 == 94ee0a96b6；版本是否 v2.9.0；标签 archive-20260923
  → 直接开工
```

**本次归档速览**：见 `docs/archive-20260923.md`
