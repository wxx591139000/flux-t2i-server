# flux-t2i-server 接交接（交接文档）

> 交接日期：**2026-09-24** · 生成方式：「归档整理」skill 的交接步骤自动产出
> 版本：**v2.10.0** · 归档标签 **`archive-20260924`**（上一份 `archive-20260923`）
>
> **续接方式（两条都行）**
> ① 说 **「按交接提示继续」** → 读本文件 + `flux-t2i-server-resume-handoff` 记忆，对齐锚点后继续
> ② 直接复制粘贴 `docs/交接提示词-新会话.md` 里「───」之间的内容

---

## 一、项目现状

| 维度 | 值 |
|---|---|
| 项目 | flux-t2i-server（FLUX 生图服务 + 任务中心 + 商户后台 + 飞书机器人「图图」） |
| 版本 | **v2.10.0** · 2026-09-24 |
| 本地路径 | `E:\myClaudCodeWorkspace\flux-t2i-server`<br>⚠️ 伞项目里的 `image-platform\flux-t2i-server` 是 **junction**，**git 操作一律走真实路径** |
| Git | `https://github.com/wxx591139000/flux-t2i-server.git` · 分支 `main` |
| **归档标签** | **`archive-20260924`** → 本次归档最终提交 |
| 公网入口 | `flux.zhuanlu.xyz` → **客户站点**（姊妹项目 image-gen-site，端口 3000）<br>`flux-admin.zhuanlu.xyz` → **商户后台**（本服务 9620，Token 保护）<br>`xhs.zhuanlu.xyz` → 小红书工具（端口 8800） |
| 本机入口 | `http://127.0.0.1:9620`（生成页）· `http://127.0.0.1:9620/admin`（商户后台，不经隧道） |
| 机群 | 唯一事实来源 = `manager/servers.json`（换机只改 JSON，不改代码） |

### 确认「是不是同一个项目」

```bash
git -C E:\myClaudCodeWorkspace\flux-t2i-server describe --tags   # 应含 archive-20260924
git -C E:\myClaudCodeWorkspace\flux-t2i-server log --oneline -3
```

---

## 二、启动方式

| 场景 | 入口 |
|---|---|
| **对外生产（推荐，一次点完）** | `image-platform\启动-对外生产站点.bat` → 9620 重启 + 隧道重启 + 3000 生产 + **身份验收** |
| 只应用隧道配置改动 | `image-platform\重启隧道.bat` |
| 只重启本服务（改代码后用这个） | `flux-t2i-server\启动-01R-FLUX服务重启.bat` |
| 只起本服务 / 小红书 / 仅隧道 | `启动-01-FLUX文生图服务.bat` / `启动-02-…` / `启动-03-仅隧道.bat` |
| 主脚本（可传参） | `start_service.ps1 -Target flux\|xhs\|tunnel\|all [-Restart]` |

⚠️ **一键启动的验收输出看这三处**：`image-platform\logs\last-run-progress.txt`（跑到哪一步）、
`last-run-verdict.txt`（结论）、`start_public-<时间戳>.log`（全程）。

⚠️ **改了任何 `.py` 都必须重启 9620 才生效**（Python import 后不再读盘）。
判据两条：① `/health` 的 uptime 远小于代码 mtime ② 新端点返回**纯文本 `Not Found`**。

---

## 三、遗留待办

### P1 剩余（图图命令面）

1. **参数状态机**（`/size /n /model`，纯文本）+ 默认参数持久化
2. **`/bind <激活码>` 绑定**（决策 2 已把它从 P5 提到 P1）——
   飞书 `open_id` 与激活码账户共享同一份额度，不做两套账
3. **待选状态机**：key 用**三元组** `(channel, chat_id, sender_id)`，
   **不要学小白的二元组**（小白后来只能靠"跨会话校验 + 冷却"打补丁）
   —— 且必须有**回归测试**（对齐小白 `test_video_omni.py`）

### 其他

- **群聊支持（P2）**：现在 `chat_type not in ('p2p','')` 直接 return
  → **群里发命令连消息都到不了图图**。用户已拍板「只服务 owner 自己 + owner 的群」（白名单）
- **图生图（P3）**：`message_type != 'text'` 直接 return → 发图不可用
- `9620 /health` 不透传 `profile` / `capabilities`
- **Qwen 显存实测仍未做**（bf16 约 30-35GB vs 32G 卡）—— 最大未知数，等带卡
- **`pause` 卡死编排器**：根因（数组 splatting 绑不上 `-NoPause`）已修，
  但"真实控制台不再卡"**未能亲自验证**（工具 stdin 是管道，复现不出）→ 下次真实运行取证

---

## 四、硬约束（务必遵守）

**既有（用户历次指定，继续有效）**

1. 🔑 LLM/HF/激活码 key 读 `manager/.env`，不硬编码进仓库
2. 📄 文档改动必升版本号
3. ⏰ 判断时间用 `date`，不凭日志猜
4. 🖼️ 不直接预览图片（会卡死）；改海报/图从 HTML/文本源处理
5. 🌐 现代模型链纯文本，收到图/视频/PDF 先转文本再喂模型
6. 🚫 不改转录项目 `server-pdf-converter`
7. 🔧 改飞书/API 配置前必须先确认；push 只在用户要求时
8. 🌉 **隧道双约定**：本地只跑 `xhs-tunnel`(27da88b4)，管 flux/xhs/flux-admin 三个域名；
   VPS 跑 `transcribe-bot`(1779cc80)。严禁用旧 transcribe-bot 配置重启本地 cloudflared
   （抢隧道致 upload.zhuanlu.xyz 502）。正确启动：
   `cd C:\Users\Dancing\.cloudflared && cloudflared tunnel run xhs-tunnel`

**2026-09-23 新增**

9. ⚠️ 改 `~/.cloudflared/config.yml` 后**必须重启 cloudflared** 才生效（否则跑内存里的旧 ingress）；
   ingress **顺序敏感**，`http_status:404` 兜底必须最后
10. ⚠️ **禁止 `& script.ps1 @数组`** 调子脚本（`[switch]` 全部绑不上 → `-NoPause` 失效 → `pause` 卡死编排器）
11. ⚠️ 任何会 `Start-Process` 的脚本，**开头先跑 `Repair-DuplicateEnv`**（本机有大小写重复的代理环境键，
    带重定向启动必抛）；对外长驻服务一律**带重定向**启动（无窗口 + 日志落盘）
12. ⚠️ 验收判据要**身份/新鲜度**，不要**活体** —— 自问「我什么都不做时它会不会也通过？」
13. ⚠️ 改 `server/` 下常驻文件 → **第一动作是 `watchdog\deploy_vps.py`**（否则被看门狗反向覆盖）

**2026-09-24 新增**

14. ⚠️ **图图的命令与提示词共用同一条输入通道**（飞书文本消息）→ 命令识别必须**早于**提交，
    且判据必须严格到「取消 + 序号/短码」—— 两个方向都会出事：
    顺序错 = 命令被当提示词**真扣额度出废图**；判据松 = 吞掉正常提示词
15. ⚠️ **`status in (...)` 的判据，每加一个状态都要回头审**；
    ★ **内存态集合（`set`/`dict`）最易漏** —— DB 侧加了墓碑过滤、内存池没加（本轮真缺陷就是这个）
16. ⚠️ **取消不退还额度**（与网页端 `/api/delete` 同一口径；用完即消耗，防"删了重传"）

---

## 五、续接路径

```
① 说「按交接提示继续」
     → 读 .workbuddy/memory/flux-t2i-server-resume-handoff.md（锚点/待办/约束）
     → 读本文件
     → 对齐锚点：git describe --tags 应含 archive-20260924；版本 v2.10.0
     → 直接开工

② 或：直接复制 docs/交接提示词-新会话.md 里「───」之间的内容，粘到新会话开头
```

**本次归档速览**：见 `docs/archive-20260924.md`
**历史交接文档**：`HANDOFF-2026-09-21.md` / `HANDOFF-2026-09-22.md`（旧格式，仅存档，不再维护）
