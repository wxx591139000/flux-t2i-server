# 新会话交接提示词（直接复制下面「───」之间的内容）

─────────────────────────────────────────────────────────────

我要继续维护 **image-platform（商品图工作室）**。这是一个已上线的 FLUX 生图平台，
包含两条独立的产品线。请你先读完这份交接文档，再动手：

**`E:\myClaudCodeWorkspace\flux-t2i-server\docs\HANDOFF-2026-09-21.md`**

另外这两份也请按需读：
- 归档（本次两件事的成果与实测数据）：
  `E:\myClaudCodeWorkspace\flux-t2i-server\docs\archive-20260921-active-model.md`
- 踩坑全集：`E:\myClaudCodeWorkspace\flux-t2i-server\docs\PITFALLS.md`

读完后请先向我**复述这四件事**（确认你真的读进去了，再开始干活）：
1. 系统由哪几个进程组成、各自端口、两条链的仓库路径
2. 「当前哪台 GPU 机开着」这个事实是谁维护的、怎么传到 manager
3. `active_names()` 返回 None / 空集 / 有值 三种情况，manager 分别该怎么处理
4. 当前（2026-09-21 17:15）还有什么事**尚未生效**、需要我配合做

---

然后按这个顺序执行：

**第一步（P0，必须先做）**：本机 manager（端口 9620）的进程启动于 16:33:48，
而代码改于 16:57 —— **跑着的进程不认识新写的 `filter_active` / `active_names`**。
Python 进程 import 后不再读盘，所以 active 收敛在线上还没生效。
请你明确告诉我重启 9620 的正确操作（注意：你在工具里起的后台进程回合结束会被回收，
所以必须给我在我自己终端里能跑的命令）。

**第二步（P1，需要我配合）**：当前三台 GPU 机全关机，`active=[]`。
我会去 AutoDL 控制台开一台（flux5 或 flux6）。开机后请你给我一条命令，
验证「约 2 分钟后 active 里出现那台机器名」，并确认 manager 选机走的是它。

**第三步**：等我开机验证通过后，再继续 P2 的遗留项。

---

协作方式（请严格遵守）：
- **结论先行**，不要铺垫和客套
- **每条结论附证据**：命令 / 文件路径 / 行号 / md5 / 退出码，让我能自己复跑验证
- **区分「已验证」和「没能验证」**；不确定就写「我没验证」，**不要猜**
- 失败直说，附报错原文和下一步
- **不可逆 / 对外动作**（git 提交、删除、发布）动手前先点名并等我确认
- 项目称呼用全名 flux-t2i-server / image-gen-site，**不要用 A链/B链 简称**
- 改既有文件前先备份，改完报告实际改动范围

─────────────────────────────────────────────────────────────

## 附：这份提示词为什么这样写

| 写法 | 理由 |
|---|---|
| 先读文档再动手 | 新会话没有上下文，_direct_ 开工会基于猜测。文档里有真实观测数据（端口、时间、git hash），能立刻对齐 |
| 要求**复述四件事** | 这是"是否真读进去"的可验证判据。复述错了 = 理解偏了，比直接干活再返工便宜得多 |
| P0 直接点名「进程比代码老」 | 这是本工作区**最贵的一条教训**（铁律 #1）。不点名的话，新会话会看着全绿的测试宣布"没问题"，而线上其实没生效 |
| 提示「工具里起的进程会被回收」 | 环境限制。不写清楚，新会话会反复尝试后台启动然后失败 |
| P1 明确「需要我配合」 | 开机是用户的动作，AI 做不了。写清楚避免 AI 干等或误判 |
| 附协作方式 | 用户偏好是稳定的，每次重复一遍比让 AI 猜好 |
