# 带卡模式开机后待办（flux5）

> 记录时间：2026-09-21 09:15
> 背景：flux5 以**无卡模式**开机（SSH 通、模型在、**无 GPU**），
> 代码已同步完成并验收通过；**加载模型 / 生图 / 质量对比必须等带卡模式**。
> 服务器 **21:59 自动关机**。

---

## 当前已完成（无卡模式下能做的都做完了）

| 项目 | 状态 | 证据 |
|---|---|---|
| flux5 连通性 | ✅ | SSH 通、注册表 `westc:27602` 有效 |
| 模型在位 | ✅ | `/root/klein-models/FLUX.2-klein-4B`（15G，含 `DOWNLOAD_DONE`） |
| **蒸馏版确认** | ✅ | `model_index.json` → `"is_distilled": true`、`_class_name: Flux2KleinPipeline` |
| 新代码上机 | ✅ | `flux_resident_server.py` `64e1c634` / `start_resident.sh` `f8338d35`，远端 = 本地 |
| **画像表远端实跑** | ✅ | klein→`(4,8,1.0)`、dev→`(25,100,3.5)`、未知→退回默认 |
| VPS mirror 同步 | ✅ | `/opt/flux-watchdog/mirror/` = `64e1c634`（含 `MODEL_PROFILES`） |
| 看门狗健康 | ✅ | systemd active，自测 5/5 |

---

## ⚠️ 带卡模式开机后，按顺序做这四件事

### ① 确认机器状态（换端口就改 `manager/servers.json`，别动代码）

```bash
py -3.11 manager/flux_resident_client.py servers
```
期望：`flux5` 可达 ✓ / **带卡 ✓** / 模型 ✓。

### ② 复验代码没被回滚（看门狗可能又同步过）

```bash
py -3.11 tools/verify_deploy.py --server flux5
```
期望：两次哈希都 PASS + 四项实跑断言全 PASS，退出码 0。

> 如果 FAIL 且远端是旧 md5 → **先查 VPS mirror**：
> `ssh vps-aliyun "md5sum /opt/flux-watchdog/mirror/*"`
> mirror 是旧的就先 `py -3.11 watchdog/deploy_vps.py`，再等 60s。

### ③ 拉起常驻，等模型加载

看门狗通常会自动拉起（每 60s 轮询）。也可手动确认：

```bash
# 看门狗视角
ssh vps-aliyun 'bash /opt/flux-watchdog/selftest_ready.sh'
```

**关键验收点**：`/health` 应出现 `profile` 字段：

```bash
py -3.11 tools/_inspect_gpu.py --server flux5
# 或直接 curl（在机器上）
curl -s http://127.0.0.1:9630/health
```

期望看到：
```json
"class_name": "Flux2KleinPipeline",
"profile": {"default_steps": 4, "max_steps": 8, "guidance": 1.0, "note": "..."}
```

⚠️ 若 `profile` 缺失 → 常驻还是旧进程，**必须重启**（旧代码没有画像字段）。

### ④ 端到端验收 + 质量对比

```bash
# 11 项端到端（SSH/GPU/torch/模型/拉起/文生图/图生图/日志/能力核对）
py -3.11 manager/acceptance_gpu.py --server flux5
```

**质量对比（本次修复的核心验证）**：

| 对比项 | 旧行为 | 新行为（画像生效后） |
|---|---|---|
| 默认 steps | 25（空转） | **4** |
| guidance | 3.5（错 3.5 倍） | **1.0** |

做法：同一 prompt + 同 seed，分别跑「显式 `steps=4`」与「不传 steps（走画像默认）」，
比对 **耗时** 与 **出图质量**。预期耗时显著下降、质量不降反升。

```bash
# steps 透传真机验证
py -3.11 tools/_steps_check.py --server flux5
```

---

## 仍需用户操作的事项

1. **重新提交 4 条图生图任务**（`ceff1b2c` / `2073d85b` / `64f5ed37` / `e023f76c`）
   —— 参考图不入库，manager 重启即失效，已判 failed + 退款（刻意设计）。
2. **请求 5 第 1 条（任务彻底删除）尚未实施** —— 待用户确认是否排期。

---

## 回归门清单（改动后必跑）

```bash
# 上游（flux-t2i-server）9 个测试
py -3.11 -m pytest tests/ -q

# 站点（image-gen-site）
npx tsc --noEmit && npx next build
node tools/edit-e2e-check.js          # 应 20/20
```
